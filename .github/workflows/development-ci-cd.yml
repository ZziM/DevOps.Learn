name: CI/CD-development

on:
    push:
        branches:
            - development
    workflow_dispatch:

jobs:
    build_and_test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Set up .NET
              uses: actions/setup-dotnet@v3
              with:
                dotnet-version: '8.0'

            - name: Restore dependencies
              run: dotnet restore ./Weather/Weather.sln

            - name: Build solution
              run: dotnet build ./Weather/Weather.sln --configuration Release --no-restore
